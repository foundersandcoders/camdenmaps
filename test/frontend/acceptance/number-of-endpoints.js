/*************************************************
*   NUMBER OF END POINTS TESTS
*   Description: Acceptance tests are written here
*   Use: run tests by npm test
**************************************************/

// var menu = require("../../../server/angular/menu.json");

// var errorMessage = '{"statusCode":500,"error":"Internal Server Error","message":"An internal server error occurred"}';

// (function () {
//     "use strict";

//     describe("markers on map", function () {

//         beforeEach(function() {
//             return browser.ignoreSynchronization = true;
//         });

//         function runTest (j) {
//             it("service" + menu[j].title, function () {

//             browser.get("/services/" + menu[j].title );
            
//             var text = element(by.tagName("pre"));

//              console.log(text.getText());

//             // expect(leafletmarkers.count()).toEqual(6);


//             });

//         }

//         for (var i = 0; i < 2 ; i++ ) {
//             if (menu[i].type === 'service') {
//                 runTest(i);

//             }
//         }
//     });

// }());
