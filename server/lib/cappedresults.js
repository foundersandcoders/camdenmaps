;(function() {
	"use strict";

	module.exports = function cappedResults(service) {
		return 	service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "hall for hire" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: service = "community centre" ? true 
				: false;
	};
}());